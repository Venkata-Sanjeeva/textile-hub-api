# 1. Database Configuration
<<<<<<< HEAD
# Replace 'your_database_name' with the actual name of your schema
spring.datasource.url=jdbc:mysql://localhost:3306/textile_store_db?createDatabaseIfNotExist=true

# Separate credentials
spring.datasource.username=root
spring.datasource.password=root

# 2. Driver & Hibernate Settings
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Max file size (individual file)
spring.servlet.multipart.max-file-size=10MB

# Max request size (total for the whole request including multiple files)
spring.servlet.multipart.max-request-size=10MB